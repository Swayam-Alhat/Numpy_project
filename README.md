# Image Manipulator

`Numpy` is python library to handle & process large data efficiently. It provides functionalities to manipulate data. `Array` is data structure used to store data & allows to perform operations on it.

Data is
